---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { pic, href, title, body } = Astro.props;
---

<li class="link-card">
	<a href={href}>
		<h2>
			{title}
		</h2>
		<img style="" src={pic} />
		<p>
			{body}
		</p>
	</a>
</li>
